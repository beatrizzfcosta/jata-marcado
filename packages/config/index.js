const eslintPreset = require('./eslint-preset.js');
const prettierPreset = require('./prettier-preset.js');
const typescriptPreset = require('./typescript-preset.json');

module.exports = {
  eslint: eslintPreset,
  prettier: prettierPreset,
  typescript: typescriptPreset,
}; 